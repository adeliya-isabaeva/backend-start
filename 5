ans=[]
mini=0
def per(x,osn):
    s=''
    while x>0:
        ost=x%osn
        s=str(ost)+s
        x=x//osn
    return s
sp=[]
for i in range(100000,1,-1):
    a=per(i,6)
    a=str(a)
    f=sum([int(a[x]) for x in range(len(a))])
    if f%5==0:
        a.replace('0','*')
        a.replace('3','0')
        a.replace('*','3')
        a='11'+a
    else:
        a=a+'44'
        a=a[0]+'0'+'5'+a[3:]
    r=int(a,6)
    if r>1500:
        sp.append([i,r])
print(sp)